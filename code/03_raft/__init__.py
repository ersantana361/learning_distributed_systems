"""Raft consensus algorithm implementation."""

# Note: Imports are intentionally omitted to avoid circular import issues
# when running tests directly. Use explicit imports in your code:
#   from log import RaftLog, LogEntry
#   from node import RaftNode, NodeState
#   from election import ElectionSimulator
#   from cluster import RaftCluster
